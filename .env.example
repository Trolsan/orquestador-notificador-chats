# API (FastAPI) — used in later phases
API_HOST=0.0.0.0
API_PORT=8000

# Base URL for Cypress and local HTTP clients (host machine)
API_BASE_URL=http://localhost:8000

# n8n → API URL when running inside Docker Compose (service name)
API_INTERNAL_URL=http://api:8000
